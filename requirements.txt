# ── Core pipeline dependencies ────────────────────────────────────────────
# yfinance must be >=1.7.0 — older 0.2.x versions fail silently against
# Yahoo's current API (confirmed: .info returned None for every field
# during testing). Combined with the devcontainer pinning Python to 3.12,
# every package below has a stable, pre-built install — nothing here
# should need to compile from source or hit native-library crashes again.
yfinance>=1.7.0
pandas>=2.3.3
requests>=2.32.3
beautifulsoup4>=4.12.3
lxml>=5.3.0
tenacity>=9.0.0
python-dotenv>=1.0.1

# ── Dashboard ──────────────────────────────────────────────────────────────
streamlit>=1.40.0

# ── Dev / testing ────────────────────────────────────────────────────────
pytest>=8.3.3
ruff>=0.7.4
